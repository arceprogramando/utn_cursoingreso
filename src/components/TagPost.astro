---
const { title, url, image, alt } = Astro.props;
---

<div class="flex flex-col items-center">
  <a href={url}>
    <img src={image} alt={alt} class="max-h-56" />
  </a>
  <h2 class="max-w-40 text-center">
    {title}
  </h2>
</div>
