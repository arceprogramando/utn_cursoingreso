---
const { platform, icon } = Astro.props;
import { Icon } from 'astro-icon/components';
---

<a href={platform} target="_blank" class="text-black">
  {icon ? <Icon name={icon} width={24} height={24} class="hover:underline font-bold uppercase hover:text-green-900 " /> : 'no tiene'}
</a>
